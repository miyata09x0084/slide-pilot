# LangSmith (Optional - for tracing and debugging)
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=lsv2_pt_your_langsmith_api_key_here
LANGCHAIN_PROJECT=react-agent
LANGCHAIN_ENDPOINT=https://api.smith.langchain.com

# OpenAI (Required)
OPENAI_API_KEY=sk-proj-your_openai_api_key_here

# Tavily (Required - for AI news search)
TAVILY_API_KEY=tvly-your_tavily_api_key_here

# Marp (Optional - slide output format)
SLIDE_FORMAT=pdf
# Options: pdf, png, html, or empty for .md only

# Google OAuth (Optional - for frontend authentication)
GOOGLE_REDIRECT_URI=http://localhost:2024/auth/google/callback

# Gmail API (Required for email sending)
# Note: credentials.json and token.json are generated via OAuth flow
# 1. Create OAuth 2.0 credentials in Google Cloud Console
# 2. Download credentials.json and place in backend/
# 3. Run test_gmail.py to authenticate (generates token.json)

# Supabase (Optional - for slide history and browser preview)
# Issue #24: https://github.com/user/repo/issues/24
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_SERVICE_KEY=eyJhbGc...
# Setup instructions:
# 1. Create Supabase project at https://supabase.com/dashboard
# 2. Create 'slides' table (see CLAUDE.md for SQL)
# 3. Create 'slide-files' Storage bucket (public)
# 4. Copy URL and service_role key from Settings > API

# PostgreSQL (Required for Cloud Run production environment)
# Used by LangGraph Server for persistent storage of threads and checkpoints
# For local development, 'langgraph dev' uses in-memory storage (this is not used)
# For production deployment, this MUST be set to enable thread persistence
POSTGRES_URI=postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
# Get this from Supabase Dashboard → Settings → Database → Connection string
