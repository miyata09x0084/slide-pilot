# LangSmith (Optional - for tracing and debugging)
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=lsv2_pt_your_langsmith_api_key_here
LANGCHAIN_PROJECT=react-agent
LANGCHAIN_ENDPOINT=https://api.smith.langchain.com

# OpenAI (Required)
OPENAI_API_KEY=sk-proj-your_openai_api_key_here

# Tavily (Required - for AI news search)
TAVILY_API_KEY=tvly-your_tavily_api_key_here

# Marp (Optional - slide output format)
SLIDE_FORMAT=pdf
# Options: pdf, png, html, or empty for .md only

# Google OAuth (Optional - for frontend authentication)
GOOGLE_REDIRECT_URI=http://localhost:2024/auth/google/callback

# Gmail API (Required for email sending)
# Note: credentials.json and token.json are generated via OAuth flow
# 1. Create OAuth 2.0 credentials in Google Cloud Console
# 2. Download credentials.json and place in backend/
# 3. Run test_gmail.py to authenticate (generates token.json)
