# BulletProof React移行実装方針 (Phase 2)

## 現状分析

### ✅ 既に完了している項目
- Feature-first アーキテクチャ採用（`features/`配下に機能分割）
- React Router v7 + Loader パターン実装
- TypeScript path alias設定（`@/*`, `@/features/*`, `@/shared/*`）
- Barrel exports（各featureに`index.ts`）
- テスト環境整備（Vitest + Testing Library）
- 45ファイル、約3,900行のコードベース

### ❌ 不足している項目（BulletProof React標準との差分）

| カテゴリ | 現状 | BulletProof React標準 | 優先度 |
|---------|------|---------------------|--------|
| **アプリケーション層** | `App.tsx`にルーター定義 | `app/`フォルダ（`routes/`, `provider.tsx`, `router.tsx`分離） | 🔴 高 |
| **データフェッチング** | `fetch` API + Recoil | React Query（TanStack Query） | 🔴 高 |
| **API層** | `useReactAgent`フック内に直接実装 | `features/*/api/` フォルダに分離 | 🟡 中 |
| **共通ライブラリ** | なし | `lib/`フォルダ（`api-client.ts`, `react-query.ts`, `auth.tsx`など） | 🟡 中 |
| **エラーハンドリング** | try-catch個別実装 | `ErrorBoundary`コンポーネント + グローバルハンドラー | 🟡 中 |
| **UIコンポーネント** | `shared/components`に2ファイルのみ | `components/ui/`に再利用可能コンポーネント群 | 🟢 低 |
| **環境設定** | `import.meta.env`直接参照 | `config/`フォルダに集約 | 🟢 低 |
| **型定義** | 各ファイルに分散 | `types/`フォルダに共通型集約 | 🟢 低 |

---

## 段階的移行計画（5フェーズ）

### **Phase 1: アプリケーション層の再構築** (1-2日)
**目的**: ルーティング設定を `app/` フォルダに移動し、プロバイダー管理を改善

**実装内容**:
1. `src/app/` ディレクトリ作成
   - `app/routes/` - ルート定義（現在の`App.tsx`から分離）
   - `app/provider.tsx` - `AppProvider`作成（`GoogleOAuthProvider`, `RecoilRoot`, `Suspense`, `ErrorBoundary`統合）
   - `app/router.tsx` - `createBrowserRouter`設定

2. エラー境界追加
   - `components/error/ErrorBoundary.tsx` 作成
   - 各ルートに適用

3. ローディング状態統一
   - `Suspense` fallbackコンポーネント作成

**移行戦略**:
- 既存の`App.tsx`を段階的にリファクタリング
- 動作確認後に古いファイルを削除

---

### **Phase 2: React Query導入** (2-3日)
**目的**: Recoilから段階的にReact Queryへ移行し、サーバー状態管理を改善

**実装内容**:
1. `lib/react-query.ts` 作成
   - `QueryClient`設定（デフォルトオプション、staleTime、cacheTime）
   - React Query DevTools設定（開発環境のみ）

2. API層抽出 (`features/*/api/`)
   - `features/dashboard/api/get-slides.ts` - スライド一覧取得
   - `features/slide/api/get-slide-detail.ts` - スライド詳細取得
   - `features/generation/api/create-thread.ts` - スレッド作成
   - `features/generation/api/stream-agent.ts` - SSEストリーミング

3. カスタムフック移行
   - `useSlides()` - スライド一覧取得（`dashboardLoader`置き換え）
   - `useSlideDetail()` - スライド詳細取得（`slideDetailLoader`置き換え）
   - `useReactAgent()`をReact Query対応にリファクタリング

4. Recoil段階的削除
   - React Queryでサーバー状態管理 → Recoilはクライアント状態のみに限定
   - 最終的にRecoil完全削除を検討

**移行戦略**:
- 新規機能はReact Query使用
- 既存機能は1つずつ移行（`dashboard` → `slide` → `generation`）
- Recoilは思考ステップなどUI状態管理に一時的に残す

---

### **Phase 3: API Client統一** (1日)
**目的**: `fetch`呼び出しを`lib/api-client.ts`に集約し、エラーハンドリングとインターセプターを統一

**実装内容**:
1. `lib/api-client.ts` 作成
   - Axios（または`ky`）インスタンス作成
   - ベースURL設定（環境変数から）
   - リクエストインターセプター（認証ヘッダー追加、ユーザーメール挿入）
   - レスポンスインターセプター（エラーハンドリング、401時のログインリダイレクト）

2. 全API関数をAxiosに置き換え
   - `features/*/api/*.ts`を更新
   - `useReactAgent.ts`のSSEストリーミングも統一

**移行戦略**:
- 既存の`fetch`呼び出しを段階的に置き換え
- エラーハンドリングをグローバルインターセプターに集約

---

### **Phase 4: 共通リソースの整理** (1-2日)
**目的**: 型定義・設定・ユーティリティを適切なフォルダに整理

**実装内容**:
1. `config/env.ts` 作成
   - 環境変数を型安全に管理
   - `VITE_API_URL`, `VITE_GOOGLE_CLIENT_ID`など

2. `types/index.ts` 作成
   - 共通型定義（`Message`, `UserInfo`, `SlideData`など）
   - 各featureから共通型を移動

3. `utils/` フォルダ整理
   - 共通ユーティリティ関数（日付フォーマット、文字列処理など）

4. `components/ui/` 作成
   - 再利用可能UIコンポーネント（Button, Input, Card など）
   - 現在の`shared/components`を移動・拡張

**移行戦略**:
- 既存コードの重複を削減
- Import文をpath aliasで整理

---

### **Phase 5: ルーティング改善とESLintルール追加** (1日)
**目的**: ルート定義を`app/routes/`配下に移動し、feature間の依存を防ぐESLintルール追加

**実装内容**:
1. `app/routes/` ディレクトリ構造
   ```
   app/routes/
   ├── index.tsx          # "/" ルート（Dashboard）
   ├── login.tsx          # "/login"
   ├── app/              # 認証必要なルート
   │   ├── root.tsx      # レイアウト + <Outlet />
   │   ├── generate.tsx  # "/generate/:threadId"
   │   └── slides.tsx    # "/slides/:slideId"
   ```

2. ESLintルール追加
   - `eslint-plugin-boundaries`導入
   - Feature間のimport禁止ルール
   - `shared` → `features` → `app` の一方向フローを強制

3. ドキュメント更新
   - `docs/architecture/FRONTEND_ARCHITECTURE.md`作成
   - 新しいフォルダ構造と設計原則を記載

**移行戦略**:
- ルート定義をファイルベースルーティングに近い形式に移行
- ESLintでアーキテクチャ違反を検出

---

## 移行後のディレクトリ構造

```
frontend/src/
├── app/                        # アプリケーション層（NEW）
│   ├── routes/                 # ルート定義
│   │   ├── index.tsx           # "/" (Dashboard)
│   │   ├── login.tsx           # "/login"
│   │   └── app/                # 認証必要
│   │       ├── root.tsx        # レイアウト
│   │       ├── generate.tsx    # スライド生成
│   │       └── slides.tsx      # スライド詳細
│   ├── provider.tsx            # グローバルプロバイダー
│   └── router.tsx              # ルーター設定
│
├── features/                   # 機能モジュール（既存）
│   ├── auth/
│   │   ├── api/                # API関数（NEW）
│   │   ├── components/
│   │   ├── hooks/
│   │   └── index.ts
│   ├── dashboard/
│   │   ├── api/                # API関数（NEW）
│   │   ├── components/
│   │   └── index.ts
│   ├── generation/
│   │   ├── api/                # API関数（NEW）
│   │   ├── components/
│   │   ├── hooks/
│   │   └── index.ts
│   └── slide/
│       ├── api/                # API関数（NEW）
│       ├── components/
│       └── index.ts
│
├── components/                 # 共通コンポーネント（拡張）
│   ├── ui/                     # 再利用可能UIコンポーネント（NEW）
│   │   ├── button.tsx
│   │   ├── input.tsx
│   │   └── card.tsx
│   └── error/                  # エラー表示（NEW）
│       └── ErrorBoundary.tsx
│
├── lib/                        # 共通ライブラリ（NEW）
│   ├── api-client.ts           # Axiosインスタンス
│   ├── react-query.ts          # React Query設定
│   └── auth.tsx                # 認証ユーティリティ
│
├── config/                     # 設定（NEW）
│   └── env.ts                  # 環境変数管理
│
├── types/                      # 共通型定義（NEW）
│   └── index.ts
│
├── utils/                      # ユーティリティ（NEW）
│   └── index.ts
│
├── assets/                     # 静的ファイル（既存）
└── main.tsx                    # エントリーポイント（既存）
```

---

## 依存関係の変更

### 追加パッケージ
```bash
npm install @tanstack/react-query axios
npm install -D @tanstack/react-query-devtools eslint-plugin-boundaries
```

### 削除候補パッケージ（Phase 2完了後）
```bash
npm uninstall recoil  # React Queryで代替可能な場合
```

---

## リスク管理

| リスク | 影響 | 対策 |
|--------|------|------|
| SSEストリーミングがReact Queryで正しく動作しない | 高 | カスタムフック（`useStreamingQuery`）作成 |
| Recoil削除でグローバル状態が失われる | 中 | React QueryのグローバルキャッシュとZustandで代替検討 |
| 移行中のバグ混入 | 中 | 既存テストを全て実行し、各Phase後に動作確認 |
| path aliasの変更で既存importが壊れる | 低 | VSCode自動import機能 + ESLintで検出 |

---

## 成功基準

✅ 全てのReact Router Loaderが`app/routes/`配下に移動
✅ React Query導入完了（全APIリクエストがキャッシュされる）
✅ `lib/api-client.ts`でエラーハンドリング統一
✅ ESLintでfeature間import禁止ルールが動作
✅ 既存テスト全てがパス（カバレッジ維持）
✅ ビルドサイズ増加が10%未満

---

## 参考リンク

- [BulletProof React GitHub](https://github.com/alan2207/bulletproof-react/tree/master/apps/react-vite)
- [Project Structure Documentation](https://github.com/alan2207/bulletproof-react/blob/master/docs/project-structure.md)
- [React Query Documentation](https://tanstack.com/query/latest/docs/react/overview)
- [React Router v7 Documentation](https://reactrouter.com/en/main)
