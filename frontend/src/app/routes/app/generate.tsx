/**
 * Generation Progress Route
 * Displays AI agent progress for slide generation
 */

import { GenerationProgressPage } from '@/features/generation';

export function GenerateRoute() {
  return <GenerationProgressPage />;
}
